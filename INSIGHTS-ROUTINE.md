# 毎朝の「気づきレポート」ルーティン（できたこと日記 × 労務ニュース）

## 目的

「できたこと日記」（Google Drive）と「労務ニュース」（このリポジトリの `news/`）を
Claudeが横断的に読み、両者のつながり（気づき）を見つけて毎朝メールで届ける。
例：「新人研修の企画書を作った → 先週のハラスメント防止新ガイドラインを組み込むといいかも」

これまで「ニュースを読んでもその先がない」「できたこととニュースの繋がりが見えにくい」という
課題があり、その解消のために追加した。

## 配信スケジュール

- 毎朝 **7:00（JST）** に、Claude Code on the web の Trigger（定期実行）機能から起動。
- 内部cronはUTC基準のため `0 22 * * *`（UTC 22:00 = JST 7:00）として登録している。
- `roumu-news` 本体のニュース収集ルーティン（`ROUTINE.md` 参照）とは完全に独立した、別のTrigger。
  ニュース収集の処理時刻（深夜2:00）・配信時刻（5:40 / 18:40）とは連動していない。

## データソース

1. **労務ニュース**：`news/YYYY-MM-DD.md`（このリポジトリ、デフォルトブランチ）
2. **できたこと日記**：Google Drive フォルダ
   `https://drive.google.com/drive/folders/1dD_DwKfXys_sycnoS9H9FbOWu8yhqr4P`
   タイトルに日付（`YYYY-MM-DD`）を含むファイルを検索して取得する。
   ファイル名の表記ゆれ（「〜のできごと」「〜できたこと日記」など）があるため、
   完全一致ではなく `title contains` で検索している。

「昨日」の基準日は、実行時にJSTで計算する（`TZ=Asia/Tokyo date -d yesterday +%F`）。
その日のファイルが見つからない場合は、直近で見つかる日付のもので代替し、その旨をレポートに明記する。

## プライバシーへの配慮

できたこと日記には、仕事の成果だけでなく、人間関係・体調・転職活動など
非常に個人的な内容も含まれている。このルーティンは日記全文を分析対象にはせず、
**「## 今日のできごと」「## できたこと」など、行動・気づきの箇条書き部分だけ**を読む。
「## 原文ママ」「## チャッピーからのコメント」に含まれるプライベートな内容は
レポート本文に引用・転載しない設計にしている。

## 実行内容（プロンプト仕様）

1. `add_repo`（このリポジトリ、read権限）でGitHubアクセスを確保する。
2. JST基準で「昨日」の日付を確定する。
3. `news/{昨日の日付}.md` を取得する。
4. Google Driveの日記フォルダから、同じ日付のファイルを検索・取得する。
5. 日記の「できたこと」項目と、その日の労務ニュース記事を照らし合わせ、
   実務・学習（社労士試験勉強を含む）に活きそうな具体的なつながりを1〜3個見つける。
   無理につなげず、見つからない日は正直にその旨を書く。
6. 以下の構成でメールを作成し、Gmail（`harubaru74@gmail.com`）に送信する：
   - 件名：「🌅 {日付}の気づきレポート」
   - 昨日のできたこと（簡潔に箇条書き）
   - 昨日の労務ニュース（見出しのみ）
   - 気づき：できたこと×ニュースのつながり
   - 一言

## 運用上の注意

- 毎回、新しいセッションで起動する（`create_new_session_on_fire: true`）。
  会話の履歴には依存しないので、手順・データソースはすべてこのファイルとTriggerのプロンプトに
  明記してある。
- Google Drive／Gmail への接続は、claude.ai のコネクタ（Gmail / Google Drive）経由。
  追加のAPIキー管理は不要。
- 将来的な拡張案（保留中）：
  - 気づきの内容をGoogle Sheetsに1行ずつ記録し、週次・月次のサマリーを別Triggerで生成する。
    Drive接続はシートへの行追記に対応していないため、これを実装する場合は
    Google Apps ScriptのWeb App（Claudeからは`WebFetch`でPOSTするだけ）を仲介させるのが現実的。
  - 配信先をLINE（既存のGAS配信経路）にも広げる。
- このルーティン自体の課金は、Anthropic APIの従量課金ではなく、Claude（Claude Code）契約プランの
  利用枠を消費する形。処理が軽い（Web検索なし）ため、既存のニュース収集ルーティンほどは
  利用枠を圧迫しない想定。
